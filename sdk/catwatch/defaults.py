DEFAULT_SERVER = "https://heyaabis--dronecat-web.modal.run"
FRAME_INTERVAL = 0.066      # ~15 FPS
FORCE_SEND_MS = 150         # force-send if no motion after 150ms
JPEG_QUALITY = 70           # lower quality = smaller frames, faster sends
STREAM_WIDTH = 640          # downscale to 640px wide before encoding
BLUR_THRESHOLD = 100.0
MOTION_THRESHOLD = 1.0
